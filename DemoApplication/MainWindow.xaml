<Window x:Class="DemoApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:rxaml="clr-namespace:ReactiveXAML;assembly=ReactiveXAML"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:self="clr-namespace:DemoApplication"
        xmlns:markupExtensions="clr-namespace:ReactiveXAML.MarkupExtensions;assembly=ReactiveXAML"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525" d:DataContext="{d:DesignInstance self:MainViewModel}">
    <Window.Resources>

    </Window.Resources>
    <StackPanel Orientation="Vertical">
        <StackPanel Orientation="Horizontal">
            <StackPanel.Resources>
                <rxaml:ObservableBinding x:Key="SampleBinding"
                                         Getter="{Binding TestSubject}"
                                         Setter="{Binding TestSubject}" />
            </StackPanel.Resources>
            <TextBox Text="{Binding Source={StaticResource SampleBinding}, Path=Value}">
            </TextBox>
            <TextBox Text="{Binding Source={StaticResource SampleBinding}, Path=Value}"></TextBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal">
            <TextBox Text="{markupExtensions:ObservableBinding GetterBinding={Binding TestSubject}, SetterBinding={Binding TestSubject}}" />
        </StackPanel>
    </StackPanel>
</Window>
